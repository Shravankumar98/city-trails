---
const { heroImage } = Astro.props;
import Button from "./ui/Button.jsx";
---

<section
  class="relative h-[360px] sm:h-[420px] md:h-[500px] flex items-center justify-center text-white"
  style={`background-image: url(${heroImage});
          background-size: cover;
          background-position: center;`}
>
  <div class="absolute inset-0 bg-black/60"></div>
  <div class="relative z-10 max-w-4xl px-4 text-center">
    <h1 class="mb-4 text-3xl sm:text-4xl md:text-5xl font-bold tracking-wide">
      Welcome to Bangalore
    </h1>
    <p class="mx-auto mb-6 max-w-2xl text-base sm:text-lg text-gray-200">
      Discover the Garden City of India — heritage, culture, and innovation
    </p>
    <Button href="#places" size="md">
      Explore Places
    </Button>
  </div>
</section>
